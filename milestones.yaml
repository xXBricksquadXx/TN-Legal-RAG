phases:
  - id: ingest-baseline
    title: Baseline TN Sunshine ingest
    done: true
    notes:
      - seeded TPRA, OORC guidelines, quickref, regs
      - vector store rebuilt

  - id: ingest-fees
    title: Fee schedule (OORC) & QA anchors
    done: true
    deliverables:
      - oorc-schedule-of-reasonable-charges.md with Q&A anchors

  - id: retrieval-tuning
    title: Retrieval scoring tune for fee Q&A
    done: true
    params:
      TOP_K: 24
      MAX_DISTANCE: 0.90
      HYBRID_ALPHA: 1.0

  - id: herdocs
    title: HerDocs county/city policy set
    done: false
    plan:
      - import selected city/county policies (HTML/PDF) via tools/import_url.py
      - normalize front-matter (topic: sunshine, jurisdiction: TN)
      - add high-signal Q&A anchors per doc
      - reindex + smoke tests

  - id: deploy
    title: Caddy reverse-proxy + systemd
    done: false
    plan:
      - systemd unit for uvicorn
      - Caddyfile: TLS, /health, /docs (if exposed)
      - Allow only LAN / VPN if needed

